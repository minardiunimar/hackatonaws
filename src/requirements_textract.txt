# Dependências para o Processador de Documentos com AWS Textract

# AWS SDK
boto3>=1.26.0
botocore>=1.29.0

# Processamento de PDF
PyMuPDF>=1.21.0

# Processamento de imagens
opencv-python>=4.7.0
Pillow>=9.4.0
numpy>=1.24.0

# Utilitários
argparse  # Já incluído no Python 3.2+
logging   # Já incluído na biblioteca padrão
re        # Já incluído na biblioteca padrão
json      # Já incluído na biblioteca padrão
os        # Já incluído na biblioteca padrão
sys       # Já incluído na biblioteca padrão
typing    # Já incluído no Python 3.5+
